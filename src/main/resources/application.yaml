spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/drimansy
    username: postgres
    password: pensemos
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    generate-ddl: true
    show-sql: true